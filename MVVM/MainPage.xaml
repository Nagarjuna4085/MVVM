<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodel="clr-namespace:MVVM"
             x:DataType="viewmodel:MainViewModel"
             x:Class="MVVM.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25"
            VerticalOptions="Center"
            >
            <Entry  Placeholder="FirstName" Text="{Binding FirstName}"   />
            <Entry  Placeholder="LastName"  Text="{Binding LastName}"/>

            <Label TextColor="White" Text="{Binding FullName}"  />
            <Button Text="Click Me!!" IsEnabled="{Binding IsNotBusy}" Command="{Binding TapCommand}"  HorizontalOptions="Center"  />


        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
